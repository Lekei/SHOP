@model HomeViewModel

<h1>Лучшие автомобили</h1>

<div class="row mt-5 mb-2">
    @{ 
        foreach (Spate spate in Model.favSpates)
            {
                @Html.Partial("AllSpates",spate)
            }  
    }

</div>